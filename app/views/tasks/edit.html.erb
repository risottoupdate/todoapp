<%= form_for @task, url: task_path(@task.id), html:{method: :patch} do |f| %>
  <p>投稿</p>
  <p>タスク：<%= f.text_field :task %></p>
  <p>状態：<%= f.text_field :state %></p>
  <p>期限：<%= f.date_field :limit_date %></p>
  <input type="submit" value="送信">
<% end %>